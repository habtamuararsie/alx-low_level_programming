#include "main.h"
/**
 * mul - Multiplies two integers.
 * @num1: The first integer to be multiply.
 * @num2: The second integer to be multiply.
 *
 * Return: The result of the multiplication.
 */
int mul(int num1, int num2)
{
	return (num1 * num2);
}
